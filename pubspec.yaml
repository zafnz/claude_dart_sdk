name: claude_agent
description: Dart SDK for Claude Code Agent - provides native Flutter interface to Claude Agent SDK
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.11.0
  uuid: ^4.0.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
